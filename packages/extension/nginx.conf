server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Twitch Extension panels are single static bundles — no client-side
    # routing to fall back for, so this is just plain static file serving.
    location / {
        try_files $uri $uri/ =404;
    }
}
